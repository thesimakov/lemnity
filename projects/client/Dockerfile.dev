# Development Dockerfile для React клиента
FROM node:20.19.0-alpine

# Устанавливаем рабочую директорию
WORKDIR /client

# Устанавливаем pnpm глобально
RUN npm install -g pnpm@10.21.0

# Открываем порт для Vite dev server
EXPOSE 5173

# Запускаем dev server с hot reload
CMD ["sh", "-c", "pnpm --filter client... --filter @lemnity/embed-script... install --frozen-lockfile && pnpm --filter client run start:dev -- --host 0.0.0.0"]
