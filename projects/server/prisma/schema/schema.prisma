generator client {
  provider = "prisma-client-js"
  
  // в монорепо в режиме разработки не работает корректно
  // нужно настраивать nest assets или генерить прямо в node_modules
  //output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
